# StereoOccNet × nuScenes 轨迹预测：对齐方案深度解析

> 深度长文：从零理解 nuScenes 轨迹预测数据格式，详解如何将 capture_v2 打造成支持轨迹真值的数据集生成引擎

---

## 一、写在前面：为什么需要对齐 nuScenes？

在自动驾驶领域，**轨迹预测（Trajectory Prediction）** 是一个承上启下的关键任务：

- **承上**：接收感知模块输出的周围物体位置、速度、类别
- **启下**：为下游规划控制模块提供未来轨迹假设

nuScenes 是目前最流行的多任务自动驾驶数据集之一，其轨迹预测任务（nuScenes Prediction）定义了行业标准的数据格式。如果你的数据集要对齐 nuScenes，意味着：

1. **模型可以直接复用 nuScenes 生态的工具链**（mmdetection3d, nuscenes-devkit）
2. **benchmark 对比有据可循**：直接对标 PointMotionNet, CoverNet, TNT 等基线
3. **数据集质量有参照系**：nuScenes 的标注精度是经过学术界验证的

本文将作为 **capture_v2 数据生成器的重构蓝图**，让这套 Isaac Sim 原生采集系统输出与 nuScenes 格式兼容的轨迹预测数据。

---

## 二、nuScenes 轨迹预测数据格式详解

### 2.1 nuScenes 数据组织的核心概念

nuScenes 用 **Token** 作为唯一标识符串联整个数据集：

| 概念 | 说明 | 类比 |
|------|------|------|
| **scene** | 一个连续采集片段（约 20 秒） | capture_v2 的一次运行 |
| **sample** | 某一时刻的采集帧 | 我们的 frame_XXXXXX |
| **sample_data** | 某sensor在该时刻的数据（图像/LIDAR） | 我们的 DNG 文件 |
| **instance** | 一个物理object的唯一标识（跨帧不变） | 我们的 instance_id |
| **annotation** | 某一时刻某 instance 的标注（bbox, velocity） | 轨迹预测需要的所有信息 |

**关键洞察**：nuScenes 的 annotation 是**逐 instance × 逐 sample** 的，即每个物体在每帧都有独立标注文件。这和你的 `trajectory/frame_XXXXXX_traj.json` 完全一致。

### 2.2 nuScenes 轨迹预测任务的核心数据

nuScenes Prediction 任务的本质是：

> 给定当前时刻 t 的所有 agent 状态，预测每个 agent 未来 6 秒的轨迹。

预测的格式要求：

```python
{
    "instance": "instance_token",      # 物体唯一标识
    "sample": "sample_token",         # 当前帧标识
    "prediction": np.ndarray,         # [num_modes, n_timesteps, 2]
                                     # num_modes: 多模态轨迹数量（通常 1）
                                     # n_timesteps: 时间步数（6s / 0.5s = 12 步）
                                     # 2: (x, y) 世界坐标
    "probabilities": np.ndarray       # 每条轨迹的概率（多模态时用）
}
```

**注意**：nuScenes 提交的是**模型预测结果**，而我们需要生成的是**真值（ground truth）**。格式完全一样，只是 `prediction` 存储的是 ground truth 轨迹。

### 2.3 你的数据集要存储的逐帧逐物体标注

根据 nuScenes annotation 格式的启发，结合你的俯视Occupancy场景，每个物体需要存储：

```json
{
    "instance_id": 1,                  // 物体唯一ID（跨帧一致）
    "category_name": "vehicle.car",    // nuScenes 格式的类别名
    "translation": [1.2, -0.5, 0.15],  // 质心位置 (x, y, z) 世界坐标
    "size": [4.5, 1.8, 1.5],          // 长宽高 (l, w, h)
    "rotation": [0, 0, 0.707, 0.707], // 四元数 (qx, qy, qz, qw)
    "velocity": [1.5, -0.3],           // 线速度 (vx, vy) m/s
    "angular_velocity": 0.05,         // 角速度 rad/s
    "is_stationary": false,           // 是否静止
    
    // ========== 轨迹预测专用 ==========
    "past_positions": [                // 过去 1 秒（可选项）
        {"t": -1.0, "translation": [0.7, -0.2, 0.15]},
        {"t": -0.5, "translation": [0.95, -0.35, 0.15]}
    ],
    "future_positions": [              // 未来 3 秒（核心）
        {"t": 0.5, "translation": [1.45, -0.65, 0.15]},
        {"t": 1.0, "translation": [1.70, -0.80, 0.15]},
        {"t": 2.0, "translation": [2.20, -1.10, 0.15]},
        {"t": 3.0, "translation": [2.70, -1.40, 0.15]}
    ],
    "num_occupied_voxels": 243,        // 该物体占用的体素数量
    "occlusion_ratio": 0.15            // 遮挡比例 [0, 1]
}
```

### 2.4 和方向预测.md 的格式对比

| 字段 | 方向预测.md | nuScenes Style | capture_v2 现状 |
|------|------------|----------------|----------------|
| instance_id | ✅ | ✅ | ✅ 已实现 |
| translation / position | ✅ bbox_center | ✅ (x, y, z) | ❌ 只存了体素索引 |
| size / bbox_size | ✅ | ✅ | ❌ 用圆柱近似 |
| rotation / heading | ✅ heading_rad | ✅ quaternion | ⚠️ npc_tracker 有方法但未输出 |
| velocity | ✅ | ✅ | ⚠️ 有计算但未写入 traj |
| future_positions | ✅ | ✅ | ❌ 完全缺失 |
| past_positions | ✅ | ✅ (可选) | ❌ 完全缺失 |
| occlusion_ratio | ✅ | ❌ (nuScenes 无此字段) | ❌ 完全缺失 |

**关键差距**：方向预测.md 的格式更接近 nuScenes，但 capture_v2 **只实现了体素级数据，没有生成逐物体的标注 JSON**。

---

## 三、capture_v2 当前数据能力评估

### 3.1 已实现的数据

```
output_v2/
├── calibration.json          ✅ 相机内参 + 外参
├── voxel_config.json        ✅ 体素网格参数
├── instance_meta.json       ✅ 全局实例注册表（ID 范围、跨帧追踪）
├── left_dng/                ✅ 左眼 DNG
├── right_dng/               ✅ 右眼 DNG
├── voxel/
│   ├── frame_*_semantic.npz ✅ 语义体素
│   ├── frame_*_instance.npz ✅ 实例体素
│   └── frame_*_flow.npz     ✅ Flow 体素 + mask
├── meta/
│   └── frame_*_json         ✅ 帧级元数据（部分字段）
└── trajectory/             ❌ 目录存在但为空
```

### 3.2 核心模块的能力

| 模块 | 已实现能力 | 未实现能力 |
|------|-----------|-----------|
| **NPCTracker** | ✅ 记录每个采集帧的 NPC 世界坐标<br>✅ 计算瞬时速度<br>✅ 计算朝向角 (get_heading_rad)<br>✅ 保存 npc_history.json | ❌ 从位置历史提取 past/future 轨迹<br>❌ 计算角速度<br>❌ 输出逐帧逐物体的 traj.json |
| **InstanceRegistry** | ✅ 按类别分配 instance_id<br>✅ 跨帧追踪同一物体<br>✅ 输出 instance_meta.json | ❌ 关联 instance_id → 当前帧位置<br>❌ 输出逐帧逐物体的标注 |
| **VoxelGridV2** | ✅ 存储 semantic + instance + flow<br>✅ flow_mask 区分动态/静态 | ❌ 从 instance.npz 统计每个物体的体素数<br>❌ 计算 bbox (需要逐物体几何) |
| **voxel_filling** | ✅ 静态物体 PhysX 查询<br>✅ NPC 圆柱体标记 | ❌ 获取 NPC 真实包围盒（圆柱 ≠ 真实形状）<br>❌ 获取 NPC 旋转/朝向 |

### 3.3 关键数据缺口分析

```
缺口 1: NPC 四元数朝向
├── 需要: rotation quaternion (qx, qy, qz, qw)
├── 当前: stamp_npc_voxels 用位置差分算 heading_rad（仅 yaw）
├── Isaac Sim: NPC prim 有 xformOp:orient，可以直接读取
└── 影响: 轨迹预测需要完整朝向

缺口 2: NPC 包围盒尺寸
├── 需要: 长宽高 (l, w, h)
├── 当前: 用固定圆柱 (NPC_RADIUS_M=0.25, NPC_HEIGHT_M=1.8)
├── Isaac Sim: NPC 骨骼模型有真实尺寸，可从 USD 获取
└── 影响: nuScenes 格式要求 size 字段

缺口 3: past_positions 和 future_positions
├── 需要: 物体过去 1s 和未来 3s 的位置序列
├── 当前: NPCTracker 只记录当前位置，不记录轨迹
├── Isaac Sim: 需要仿真器跑完整个轨迹后回填
└── 影响: 轨迹预测训练必须的数据

缺口 4: occlusion_ratio
├── 需要: 物体被遮挡的比例
├── 当前: 完全未实现
├── Isaac Sim: 可以通过射线投射到相机计算可见比例
└── 影响: 数据质量分析/训练采样权重

缺口 5: angular_velocity
├── 需要: 角速度 (rad/s)
├── 当前: NPCTracker 有方法签名但未实现
├── Isaac Sim: 从连续两帧的四元数差分计算
└── 影响: 完整运动状态描述
```

---

## 四、从 Isaac Sim 获取轨迹数据的完整方案

### 4.1 数据获取的两种范式

| 范式 | 原理 | 优点 | 缺点 | 推荐场景 |
|------|------|------|------|---------|
| **实时采集** | 每帧同时采集图像 + 体素 + 位置 | 一趟完成 | 需要预知轨迹长度 | 短序列（<5s） |
| **两趟分离** | 第一趟只录位置历史，第二趟生成轨迹文件 | 逻辑清晰，可离线处理 | 需要两次仿真 | 长序列/复杂轨迹 |

**推荐两趟分离**：
1. **第一趟（采集）**：capture_v2 运行，NPCTracker 保存完整 `npc_history.json`
2. **第二趟（后处理）**：离线脚本从 `npc_history.json` 生成所有 `trajectory/*.json`

这个设计也和 nuScenes 的理念一致：**数据采集和标注解耦**。

### 4.2 第一趟：采集阶段需要增强的内容

#### 4.2.1 增强 NPCTracker：添加朝向和包围盒追踪

当前 NPCTracker 只追踪位置，需要扩展：

```python
class NPCTracker:
    def __init__(self, ...):
        # 新增
        self._orientations: dict[int, deque] = {}  # 四元数历史
        self._sizes: dict[int, tuple] = {}          # 固定尺寸 (l, w, h)
    
    def update(self, stage, stage_mpu, sim_step):
        """增强：同时读取位置 + 朝向"""
        positions = get_npc_world_positions(stage, stage_mpu)
        quaternions = get_npc_world_orientations(stage, stage_mpu)  # 新增
        
        # ... 存入 _orientations
        
        # 如果是新出现的 NPC，读取并缓存其尺寸
        for idx, pos in enumerate(positions):
            if idx not in self._sizes:
                size = get_npc_bounding_box(stage, idx, stage_mpu)
                self._sizes[idx] = size
```

#### 4.2.2 新增 Isaac Sim 数据获取函数

在 `voxel_filling.py` 中新增：

```python
def get_npc_world_orientations(stage, stage_mpu: float) -> list[np.ndarray]:
    """获取所有 NPC 的世界系四元数朝向。
    
    Returns:
        list of np.array(4,): 每个 NPC 的 (qx, qy, qz, qw)
    """
    from pxr import UsdGeom, Gf
    
    orientations = []
    chars_prim = stage.GetPrimAtPath("/World/Characters")
    if not chars_prim.IsValid():
        return orientations
    
    for child in chars_prim.GetChildren():
        if not child.GetName().startswith("Character"):
            continue
        xformable = UsdGeom.Xformable(child)
        try:
            xf = xformable.ComputeLocalToWorldTransform(0)
            rot = xf.ExtractRotationQuat()
            orientations.append(np.array([rot.x, rot.y, rot.z, rot.w]))
        except Exception:
            orientations.append(np.array([0, 0, 0, 1]))  # 默认朝 +X
    return orientations


def get_npc_bounding_box(stage, npc_index: int, stage_mpu: float) -> tuple[float, float, float]:
    """从 NPC prim 的可见几何估算包围盒。
    
    方案A（推荐）: 从 USD prim 的 Extent attribute 读取
    方案B: 用固定值 (NPC_RADIUS_M, NPC_RADIUS_M, NPC_HEIGHT_M)
    
    Returns:
        (length, width, height) 米
    """
    from pxr import UsdGeom
    
    npc_prim_path = f"/World/Characters/{_get_npc_name(npc_index)}"
    npc_prim = stage.GetPrimAtPath(npc_prim_path)
    
    if not npc_prim.IsValid():
        return (NPC_RADIUS_M * 2, NPC_RADIUS_M * 2, NPC_HEIGHT_M)
    
    # 方案A: 尝试从 prim 的 Extent 读取
    extent_attr = npc_prim.GetAttribute("extent")
    if extent_attr and extent_attr.IsValid():
        extent = extent_attr.Get()
        if extent and len(extent) == 2:
            min_pt = np.array(extent[0]) / stage_mpu
            max_pt = np.array(extent[1]) / stage_mpu
            size = max_pt - min_pt
            return (float(size[0]), float(size[1]), float(size[2]))
    
    # 方案B: 回退到圆柱近似
    return (NPC_RADIUS_M * 2, NPC_RADIUS_M * 2, NPC_HEIGHT_M)
```

#### 4.2.3 增强采集循环：保存完整 NPC 状态

在 `capture_frame()` 函数中：

```python
def capture_frame(frame_id, sim_step):
    # ... 现有代码 ...
    
    # 新增: 收集本帧所有 NPC 的完整状态
    npc_states = []
    for idx, pos in enumerate(npc_positions):
        state = {
            "npc_index": idx,
            "position": pos.tolist(),
            "velocity": npc_tracker.get_velocity(idx, sim_step, capture_interval).tolist(),
            "heading": npc_tracker.get_heading_rad(idx),
            "angular_velocity": npc_tracker.get_angular_velocity(idx),  # 新增
            "bbox_size": instance_registry.get_npc_size(idx),            # 新增
        }
        npc_states.append(state)
    
    # 保存到帧级数据结构（后续后处理使用）
    npc_tracker.append_frame_state(sim_step, npc_states)
```

### 4.3 第二趟：后处理生成轨迹文件

#### 4.3.1 轨迹生成算法

从 `npc_history.json` 提取每帧的 past 和 future 轨迹：

```python
def generate_trajectory_json(history: dict, frame_data: list, 
                             output_dir: str, sim_fps: float):
    """从位置历史生成逐帧轨迹 JSON。
    
    Args:
        history: NPCTracker.save_history() 保存的 JSON
        frame_data: 采集时的帧级数据（含每帧 NPC 状态）
        output_dir: trajectory/ 目录
        sim_fps: 仿真帧率
    """
    import json
    from pathlib import Path
    
    TRAJ_FUTURE_SEC = 3.0   # 预测未来 3 秒
    TRAJ_PAST_SEC = 1.0     # 参考过去 1 秒
    TRAJ_INTERVAL_SEC = 0.5 # 轨迹采样间隔
    
    records = history["records"]  # [{sim_step, timestamp, positions}, ...]
    
    # 构建 sim_step → timestamp 映射
    step_to_timestamp = {r["sim_step"]: r["timestamp_sec"] for r in records}
    step_to_positions = {r["sim_step"]: r["positions"] for r in records}
    
    for frame_info in frame_data:
        sim_step = frame_info["sim_step"]
        frame_str = frame_info["frame_str"]
        current_time = step_to_timestamp.get(sim_step, 0)
        
        objects = []
        for npc_state in frame_info["npc_states"]:
            idx = npc_state["npc_index"]
            
            # 过去轨迹
            past_positions = []
            for t_offset in [-1.0, -0.5]:
                t = current_time + t_offset
                nearest_step = _find_nearest_step(step_to_timestamp, t)
                if nearest_step is not None:
                    pos = step_to_positions.get(nearest_step, [None] * len(idx))
                    past_positions.append({
                        "t": t_offset,
                        "translation": pos[idx] if pos[idx] is not None else [0, 0, 0]
                    })
            
            # 未来轨迹
            future_positions = []
            for t_offset in [0.5, 1.0, 2.0, 3.0]:
                t = current_time + t_offset
                nearest_step = _find_nearest_step(step_to_timestamp, t)
                if nearest_step is not None:
                    pos = step_to_positions.get(nearest_step, [None] * len(idx))
                    future_positions.append({
                        "t": t_offset,
                        "translation": pos[idx] if pos[idx] is not None else [0, 0, 0]
                    })
            
            # 计算质心（当前位置 = 过去轨迹最后一个点）
            centroid = npc_state["position"]
            
            # 构建物体标注
            obj = {
                "instance_id": _npc_index_to_instance_id(idx),  # 映射到 InstanceRegistry 的 ID
                "category_name": _npc_index_to_category(idx),
                "translation": centroid,
                "size": npc_state["bbox_size"],
                "rotation": _heading_to_quaternion(npc_state["heading"]),
                "velocity": [npc_state["velocity"][0], npc_state["velocity"][1]],
                "angular_velocity": npc_state["angular_velocity"],
                "is_stationary": _is_stationary(npc_state["velocity"]),
                "past_positions": past_positions,
                "future_positions": future_positions,
                "num_occupied_voxels": frame_info["occupied_voxel_counts"].get(idx, 0),
                "occlusion_ratio": 0.0,  # Isaac Sim 场景无遮挡，先设为 0
            }
            objects.append(obj)
        
        # 保存
        traj_path = Path(output_dir) / f"{frame_str}_traj.json"
        with open(traj_path, "w") as f:
            json.dump({
                "frame_id": frame_str,
                "timestamp_sec": current_time,
                "objects": objects
            }, f, indent=2)


def _find_nearest_step(step_to_ts, target_ts):
    """找到最接近目标时间戳的 sim_step。"""
    nearest = None
    min_diff = float('inf')
    for step, ts in step_to_ts.items():
        diff = abs(ts - target_ts)
        if diff < min_diff:
            min_diff = diff
            nearest = step
    return nearest if min_diff < 0.5 else None  # 阈值 0.5s


def _heading_to_quaternion(heading_rad: float) -> list:
    """heading 角 → 四元数 (nuScenes 约定: 绕 Z 轴旋转)"""
    import math
    half = heading_rad / 2
    return [0, 0, math.sin(half), math.cos(half)]


def _is_stationary(velocity) -> bool:
    import numpy as np
    speed = np.linalg.norm(velocity[:2])
    return speed < 0.1  # < 0.1 m/s 视为静止
```

### 4.4 occlusion_ratio 计算方案

虽然 Isaac Sim 场景中物体天然无遮挡（仿真器可以精确渲染），但语义层面的遮挡仍需要计算：

```python
def compute_occlusion_ratio(voxel_grid, instance_id: int, 
                            instance_registry) -> float:
    """计算某实例被遮挡的比例。
    
    方法: 从 instance.npz 统计该 instance_id 的体素数，
          与 instance_meta.json 中记录的该物体总尺寸估算的体素数对比。
    """
    import numpy as np
    
    # 统计 instance.npz 中该 instance_id 占用的体素数
    actual_voxels = np.sum(voxel_grid.instance == instance_id)
    
    # 从 instance_meta.json 读取物体典型尺寸
    meta = instance_registry.get_meta(instance_id)
    size = meta.get("typical_size", [0.5, 0.5, 1.7])
    
    # 估算理论体素数（假设物体占满其 bbox）
    vx_size = voxel_grid.VOXEL_SIZE
    est_voxels = (size[0] / vx_size) * (size[1] / vx_size) * (size[2] / vx_size)
    
    # 遮挡比例 = 1 - 实际/理论（实际可能因为距离超出感知范围而更少）
    occlusion = max(0.0, 1.0 - actual_voxels / max(1, est_voxels))
    return float(occlusion)
```

---

## 五、重构后的完整目录结构

```
output_v3/
├── calibration.json              ✅ 相机标定
├── voxel_config.json             ✅ 体素网格配置
├── instance_meta.json            ✅ 全局实例注册表（v2）
│
├── left_dng/                     ✅
├── right_dng/                    ✅
├── left/                         ✅ PNG
├── right/                        ✅ PNG
│
├── voxel/
│   ├── frame_*_semantic.npz      ✅ 语义体素
│   ├── frame_*_instance.npz      ✅ 实例体素
│   └── frame_*_flow.npz          ✅ Flow 体素
│
├── meta/
│   └── frame_*_json              ✅ 帧级元数据（增强：含 NPC 状态统计）
│
├── trajectory/                   🆕 轨迹预测标注
│   ├── frame_000000_traj.json    🆕 逐帧逐物体标注
│   ├── frame_000001_traj.json
│   └── ...
│
├── npc_history.json              🆕 NPC 完整位置历史（采集副产物）
└── capture_config.json           🆕 采集配置快照（方便复现）
```

---

## 六、nuScenes 格式对齐清单

| 字段 | nuScenes annotation | capture_v2 trajectory | 对齐状态 |
|------|---------------------|------------------------|---------|
| instance_id | ✅ token | ✅ 数字 ID | ✅ 完全对齐 |
| category_name | ✅ 如 "vehicle.car" | ✅ 如 "pedestrian" | ⚠️ 需扩展为 nuScenes 层级 |
| translation | ✅ [x, y, z] 世界坐标 | ✅ [x, y, z] 世界坐标 | ✅ 完全对齐 |
| size | ✅ [l, w, h] | ✅ [l, w, h] | ✅ 完全对齐 |
| rotation | ✅ quaternion | ✅ quaternion | ✅ 完全对齐（新增） |
| velocity | ✅ [vx, vy] | ✅ [vx, vy] | ✅ 完全对齐（新增） |
| angular_velocity | ✅ scalar | ✅ scalar | ✅ 完全对齐（新增） |
| is_stationary | ❌ | ✅ | 🆕 扩展字段 |
| future_positions | ✅ (提交格式) | ✅ (真值格式) | ✅ 完全对齐（新增） |
| past_positions | ✅ (可选) | ✅ | 🆕 扩展字段 |
| num_occupied_voxels | ❌ | ✅ | 🆕 扩展字段 |
| occlusion_ratio | ❌ | ✅ | 🆕 扩展字段 |

---

## 七、关键技术细节

### 7.1 NPC 朝向的四元数表示

nuScenes 使用四元数表示 3D 旋转。在 Z-up 坐标系（Isaac Sim 默认）中，绕 Z 轴旋转 θ 的四元数为：

```
q = (0, 0, sin(θ/2), cos(θ/2))
```

从 `npc_tracker.get_heading_rad()` 转换：

```python
def heading_to_quaternion(heading_rad):
    """heading (rad) → nuScenes 格式 quaternion (x,y,z,w)"""
    half = heading_rad / 2.0
    return [0.0, 0.0, math.sin(half), math.cos(half)]
```

### 7.2 速度的世界坐标系 vs 体素局部坐标系

| 坐标系 | 定义 | 用途 |
|-------|------|------|
| **世界系** | 固定不变，+X=东，+Y=北，+Z=上 | nuScenes 格式、轨迹预测 |
| **体素局部系** | 原点=相机地面投影，X=前，Y=左 | flow.npz、训练输入 |

**转换**：轨迹 JSON 存世界系，flow.npz 存局部系。

### 7.3 位置历史的存储效率

当前 `NPCTracker._global_log` 每帧记录所有 NPC 位置，对于 1000 帧 × 10 NPC：

```
存储量 ≈ 1000 × 10 × 3 × 8 bytes ≈ 240 KB（JSON 文本会更大但仍可控）
```

建议使用 **numpy binary** 存储位置历史，减少后处理 IO 开销：

```python
def save_history_binary(self, path: str):
    import numpy as np
    records = self._global_log
    
    # 将所有帧的所有 NPC 位置转为 (T, max_npcs, 3) 数组
    max_npcs = max(len(r["positions"]) for r in records)
    arr = np.zeros((len(records), max_npcs, 3), dtype=np.float32)
    timestamps = np.zeros(len(records), dtype=np.float32)
    
    for i, rec in enumerate(records):
        timestamps[i] = rec["timestamp_sec"]
        for j, pos in enumerate(rec["positions"]):
            arr[i, j] = pos
    
    np.savez_compressed(
        path.replace(".json", ".npz"),
        positions=arr,
        timestamps=timestamps,
        sim_steps=[r["sim_step"] for r in records]
    )
```

---

## 八、nuScenes 生态兼容性

### 8.1 mmdetection3d 接入

如果要用 mmdetection3d 训练轨迹预测模型，需要将数据转换为 nuScenes 格式：

```python
def convert_to_nuscenes_format(output_v3_dir: str, nuscenes_root: str):
    """将 capture_v3 输出转换为 mmdetection3d 可读的 nuScenes 格式。"""
    # 1. 创建 scene, sample, sample_data 记录
    # 2. 为每个 instance 创建 instance.json 条目
    # 3. 为每帧每个 NPC 创建 annotation
    
    # 这个转换器需要完整实现 nuScenes 的 token 体系
    # 工作量较大，建议先用自定义 Dataset 类加载 capture_v3 格式
    raise NotImplementedError("See stereo_e2e_occ/datasets/capture_v3_dataset.py")
```

### 8.2 建议的数据集类设计

```python
# stereo_e2e_occ/datasets/capture_v3_dataset.py
from torch.utils.data import Dataset
from pathlib import Path
import numpy as np
import json

class CaptureV3Dataset(Dataset):
    """兼容 capture_v3 输出的数据集类。
    
    支持的任务:
    - Semantic Occupancy Prediction
    - Instance Segmentation
    - Voxel Flow Prediction
    - Trajectory Prediction (nuScenes-style)
    """
    
    TASKS = ["semantic", "instance", "flow", "trajectory"]
    
    def __init__(self, root: str, tasks: list[str] = None):
        self.root = Path(root)
        self.tasks = tasks or self.TASKS
        
        # 扫描所有帧
        self.frames = sorted([
            p.stem.replace("_semantic", "") 
            for p in (self.root / "voxel").glob("*_semantic.npz")
        ])
        
        # 加载元数据
        with open(self.root / "instance_meta.json") as f:
            self.instance_meta = json.load(f)
    
    def __len__(self):
        return len(self.frames)
    
    def __getitem__(self, idx):
        frame = self.frames[idx]
        
        result = {"frame_id": frame}
        
        if "semantic" in self.tasks:
            sem = np.load(self.root / "voxel" / f"{frame}_semantic.npz")["data"]
            result["semantic"] = sem
        
        if "flow" in self.tasks:
            flow_data = np.load(self.root / "voxel" / f"{frame}_flow.npz")
            result["flow"] = flow_data["flow"]
            result["flow_mask"] = flow_data["flow_mask"]
        
        if "trajectory" in self.tasks:
            traj = json.load(open(self.root / "trajectory" / f"{frame}_traj.json"))
            result["trajectory"] = traj
        
        return result
    
    def get_voxel_to_world_transform(self, frame: str) -> np.ndarray:
        """获取该帧的 voxel → world 变换矩阵。"""
        meta = json.load(open(self.root / "meta" / f"{frame}.json"))
        cam_pos = np.array(meta["camera_pos"])
        cam_yaw = meta["camera_yaw_rad"]
        # ... 构建变换矩阵
        return transform_matrix
```

---

## 九、重构实施路线图

### Phase 1: 数据采集增强（1-2 天）

| 任务 | 状态 | 改动文件 |
|------|------|---------|
| NPCTracker 增加朝向追踪 | 🆕 新增 | `npc_tracker.py` |
| 新增 `get_npc_world_orientations()` | 🆕 新增 | `voxel_filling.py` |
| 新增 `get_npc_bounding_box()` | 🆕 新增 | `voxel_filling.py` |
| 采集循环保存完整 NPC 状态 | 🔧 修改 | `main.py` |
| 保存采集配置 JSON | 🆕 新增 | `main.py` |

### Phase 2: 后处理脚本（1 天）

| 任务 | 状态 | 新增文件 |
|------|------|---------|
| 轨迹 JSON 生成器 | 🆕 新增 | `postprocess_trajectories.py` |
| occlusion_ratio 计算 | 🆕 新增 | `postprocess_trajectories.py` |
| 数据统计报告 | 🆕 新增 | `postprocess_trajectories.py` |

### Phase 3: 数据集类（2-3 天）

| 任务 | 状态 | 新增文件 |
|------|------|---------|
| CaptureV3Dataset 类 | 🆕 新增 | `stereo_e2e_occ/datasets/` |
| nuScenes 格式转换器（可选） | 🆕 新增 | `stereo_e2e_occ/datasets/convert_nuscenes.py` |

---

## 十、总结

| 问题 | 回答 |
|------|------|
| capture_v2 能否对齐 nuScenes 轨迹预测？ | **能**，核心数据结构（instance_id, velocity, translation）已就绪 |
| 需要新增哪些内容？ | NPC 四元数朝向、包围盒、past/future 轨迹提取脚本 |
| 数据获取方案？ | **两趟分离**：采集时记录历史，后处理生成轨迹文件 |
| 和方向预测.md 的关系？ | 方向预测.md 定义了数据格式，本文档提供从 Isaac Sim 获取这些数据的完整实现方案 |
| nuScenes 生态兼容性？ | 数据格式对齐后，可通过数据集类或格式转换器接入 mmdetection3d 等工具链 |

---

## 附录：参考链接

- [nuScenes Prediction Tutorial](https://www.nuscenes.org/tutorials/prediction_tutorial.html)
- [nuScenes Prediction Task](https://www.nuscenes.org/prediction)
- [mmdetection3d nuScenes Dataset](https://mmdetection3d.readthedocs.io/en/v0.16.0/_modules/mmdet3d/datasets/nuscenes_dataset.html)
- [Isaac Sim USD API Documentation](https://openusd.org/release/index.html)
